# Скопируй в .env и задай свой токен. .env в git не коммитится.
# Общий Bearer-токен доступа. Сгенерируй криптостойкое значение, например:
#   openssl rand -hex 32
KEYCHECKER_TOKEN=change-me

# Порт на хосте (в контейнере всегда 8080). Опционально.
KEYCHECKER_PORT=8080
